set -euo pipefail

-e Fail Fast（快速失败）。只要脚本中任何一行命令的返回状态码不是 0（表示执行失败），脚本就会立刻终止运行。
-u (nounset)未定义变量，禁止使用。只要脚本试图使用一个没有被赋值过的变量，Bash 会立刻抛出错误（`unbound variable`）并停止脚本运行。
-o pipefail 默认情况下管道符 `|` 的作用是把前一个命令的输出传给后一个。但 Bash 的默认逻辑是：整个管道链的执行结果，只取决于最后一个命令是否成功。
加上 `-o pipefail` 之后，在一条管道命令中，只要有任何一个环节执行失败（返回非 0 值），整个管道链的返回值就是失败。
这样就能完美触发 `-e` 机制，让脚本立刻停下来。
